server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/baozc/config.git
          username: baozc
          password: Flower.1210
          search-paths: mall
  application:
    name: configserver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/